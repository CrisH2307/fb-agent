# FB Agent — Observer CLI

Setup, flags, prompts, and troubleshooting are documented in the root
[README](../README.md#the-observer-observer).

Quick start:

```bash
cd observer
npm install
cp .env.example .env   # then add ANTHROPIC_API_KEY
npm run observe
```
