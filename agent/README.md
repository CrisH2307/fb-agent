# FB Agent — Web App

Setup, usage, and customization docs live in the root
[README](../README.md#the-web-app-agent).

Quick start:

```bash
cd agent
npm install
cp .env.example .env   # then add REACT_APP_ANTHROPIC_API_KEY
npm start              # http://localhost:3000
```
